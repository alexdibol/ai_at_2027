# Python Runtime

Install:
```bash
pip install -e .
```

Test:
```bash
pytest -q
```

Run:
```bash
aiat run examples/momentum_mission.json
```

The quantitative handlers are fail-closed placeholders until the validated NB00-NB10 implementations are wired in.
